{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "..",
    "outDir": "../../dist",
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "../shared/**/*.ts"],
  "exclude": [
    "test/**/*.ts",
    "**/*.spec.ts",
    "../shared/**/*.spec.ts",
    "../../node_modules",
    "../../dist"
  ]
}
